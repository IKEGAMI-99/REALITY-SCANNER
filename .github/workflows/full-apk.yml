name: Full AI APK

on:
  push:
    branches: ["main"]
    paths:
      - ".github/workflows/full-apk.yml"
      - "app/**"
      - "scripts/**"
      - "signing/**"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-full:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "9.6.0"

      - name: Install stable debug signing key
        run: |
          mkdir -p "$HOME/.android"
          base64 -d signing/reality-scanner-debug.keystore.b64 > "$HOME/.android/debug.keystore"
          chmod 600 "$HOME/.android/debug.keystore"
          keytool -list -keystore "$HOME/.android/debug.keystore" -storepass android -alias androiddebugkey >/dev/null

      - name: Install CPU PyTorch
        run: |
          python -m pip install --upgrade pip
          python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

      - name: Install YOLO export dependencies
        run: |
          python -m pip install ultralytics onnx onnxslim

      - name: Export YOLO26x compatibility model
        run: |
          python scripts/export_yolo26.py
          test -s yolo26x.onnx
          mkdir -p app/src/main/assets/models
          mv yolo26x.onnx app/src/main/assets/models/yolo26x.onnx

      - name: Export Snapdragon HTP QNN live models
        run: |
          python scripts/export_yolo26_qnn.py
          test -s yolo26s_v79_qnn.onnx
          test -s yolo26n_v79_qnn.onnx
          mv yolo26s_v79_qnn.onnx app/src/main/assets/models/yolo26s_v79_qnn.onnx
          mv yolo26n_v79_qnn.onnx app/src/main/assets/models/yolo26n_v79_qnn.onnx
          ls -lh app/src/main/assets/models/

      - name: Build full debug APK
        run: gradle :app:assembleDebug --stacktrace

      - name: Rename APK
        run: |
          cp app/build/outputs/apk/debug/app-debug.apk REALITY-SCANNER-v0.1.3-QNN-HYBRID.apk
          test -s REALITY-SCANNER-v0.1.3-QNN-HYBRID.apk
          ls -lh REALITY-SCANNER-v0.1.3-QNN-HYBRID.apk

      - uses: actions/upload-artifact@v4
        with:
          name: reality-scanner-qnn-hybrid-v0.1.3
          path: REALITY-SCANNER-v0.1.3-QNN-HYBRID.apk
          retention-days: 30

      - name: Publish GitHub Release APK
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view v0.1.3 >/dev/null 2>&1; then
            gh release upload v0.1.3 REALITY-SCANNER-v0.1.3-QNN-HYBRID.apk --clobber
          else
            gh release create v0.1.3 REALITY-SCANNER-v0.1.3-QNN-HYBRID.apk \
              --title "REALITY SCANNER v0.1.3" \
              --notes "QNN/HTP acceleration build for Snapdragon 8 Elite. Prefers YOLO26s QNN, falls back to YOLO26n QNN, then YOLO26x ONNX. Also fixes status-bar/cutout overlap."
          fi
